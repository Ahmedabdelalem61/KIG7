# Copy to .env and adjust. Used by docker-compose for Postgres only.
# db_password in configs/docker.odoo.conf must match POSTGRES_PASSWORD here.

POSTGRES_USER=odoo
POSTGRES_PASSWORD=odoo
POSTGRES_DB=postgres

# Host port published for Odoo web (8069 inside container)
ODOO_PUBLISH_PORT=8073
