# Backup artifacts placeholder.
# Place the staging snapshot here before running Deploy-Kig7.ps1:
#   kig7_db.dump        (pg_dump -Fc of 18c_hr_project_test)
#   kig7_filestore.tgz  (tar -czf of /var/lib/odoo/filestore/18c_hr_project_test)
# These contain real HR PII and are intentionally git-ignored. Transfer them
# to the client machine via a secure channel (not via git).
